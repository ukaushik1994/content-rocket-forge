import React from 'react';
// Fix: ProviderManagement is a default export
import ProviderManagement from './ProviderManagement';

const MinimalAPISettings = () => {
  return (
    <div>
      <ProviderManagement />
    </div>
  );
};

export default MinimalAPISettings;
