
# Plan: Collapse Library, Tools, and Engage by Default

Change `defaultOpen` from `true` to `false` for the Library, Tools, and Engage `CollapsibleSection` components in `ChatHistorySidebar.tsx` (lines 408, 415, 422).
